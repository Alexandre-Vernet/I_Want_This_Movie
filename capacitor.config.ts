import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'I-Want-This-Movie',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
